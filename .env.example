# Token del bot ottenuto da @BotFather
TELEGRAM_BOT_TOKEN=123456789:AAExampleTokenNotReal

# Percorso del file di configurazione (opzionale, default: config/curfew-config.json)
# CURFEW_CONFIG_PATH=config/curfew-config.json

# In alternativa al file, si può passare la configurazione come JSON inline
# (utile su Railway se non si vuole gestire un volume persistente)
# CURFEW_CONFIG_JSON={"timezone":"Europe/Rome","groups":[]}
